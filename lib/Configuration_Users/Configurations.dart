import 'package:flutter/material.dart';

class Configurations extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Configuraciones de Usuarios'),
          centerTitle: true,
          backgroundColor: Colors.deepOrangeAccent,
        ),
      );
}
